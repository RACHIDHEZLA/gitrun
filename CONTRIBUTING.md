# المساهمة في gitrun

🎉 **شكراً جزيلاً لاهتمامك بالمساهمة في gitrun!**  
كل مساهمة – كبيرة أو صغيرة – تساعد في جعل الأداة أفضل للجميع. سواء كنت تبلغ عن خطأ، تقترح ميزة جديدة، تحسن التوثيق، أو ترسل كوداً، فأنت مرحب بك دائماً! 🙏

## 📋 قواعد المساهمة

### 1. الإبلاغ عن مشكلة (Bug) أو اقتراح ميزة
- ابحث أولاً في [الـ Issues الموجودة](https://github.com/RACHIDHEZLA/gitrun/issues) للتأكد أن المشكلة لم تُبلغ من قبل.
- إذا لم تجدها، افتح Issue جديدة:
  - استخدم قالب "Bug report" للأخطاء.
  - استخدم قالب "Feature request" للميزات الجديدة.
- كن واضحاً: وصف المشكلة، خطوات إعادة إنتاجها، النظام المستخدم، والإصدار (`gitrun --version`).

### 2. إرسال تغييرات (Pull Request)
- **Fork** المستودع إلى حسابك.
- أنشئ فرعاً جديداً باسم واضح:
  ```bash
  git checkout -b fix/اسم-الإصلاح
  # أو
  git checkout -b feature/اسم-الميزة
  ```
- قم بتطبيق تغييراتك مع الالتزام بقواعد الكود (انظر أدناه).
- اكتب commit messages واضحة ومفيدة (بالإنجليزية أو العربية).
- تأكد من اجتياز الاختبارات محلياً:
  ```bash
  pytest
  ```
- Push الفرع وافتح Pull Request نحو الفرع `main`.

### 3. قواعد الكود
- اتبع **PEP 8** لتنسيق الكود.
- استخدم **type hints** في الدوال الجديدة.
- أضف **docstrings** واضحة للدوال والكلاسات.
- أضف اختبارات لأي ميزة أو إصلاح جديد (في مجلد `tests/`).
- حافظ على بساطة ووضوح الكود.

## 🛠️ إعداد بيئة التطوير

```bash
# 1. استنساخ المستودع
git clone https://github.com/RACHIDHEZLA/gitrun.git
cd gitrun

# 2. إنشاء بيئة افتراضية (موصى)
python -m venv .venv
source .venv/bin/activate    # على Linux/Mac
# أو .venv\Scripts\activate  على Windows

# 3. تثبيت الحزمة في وضع التطوير مع المتطلبات الإضافية
pip install -e ".[dev]"

# 4. تشغيل الاختبارات
pytest -v

# 5. تنسيق الكود (اختياري لكن موصى)
black gitrun/
flake8 gitrun/
```

## ✅ عملية المراجعة
- سأراجع الـ Pull Request في أقرب وقت ممكن.
- قد أطلب تعديلات صغيرة لضمان الجودة.
- بعد الموافقة، سيتم دمج التغييرات في الإصدار التالي.

## 🎁 أفكار للمساهمات المستقبلية
- دعم Bitbucket أو مستودعات خاصة (private repos) باستخدام token.
- خيار `--no-deps` لتخطي تثبيت المتطلبات.
- دعم pyproject.toml أو poetry للمتطلبات.
- إضافة دعم لـ Node.js أو لغات أخرى.
- تحسين الأداء أو التخزين المؤقت.

**شكراً لك على وقتك وجهدك – معاً نجعل gitrun أفضل!** 🚀  

إذا كان لديك أي سؤال، لا تتردد في فتح Issue أو التواصل مباشرة.

— RACHIDHEZLA
